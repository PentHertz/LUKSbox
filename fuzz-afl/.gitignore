# AFL build output and per-run state. The harness sources, seed
# corpora, and Cargo.toml ARE checked in; everything else is
# regenerated by `cargo afl build` and `scripts/fuzz_server.sh`.
target/
runs/
Cargo.lock
